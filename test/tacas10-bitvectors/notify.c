
int main(void)
{
  unsigned long length; // nondet

  // abstracted list deletion
  while (length!=0) {
      // thisEntry = RemoveHeadList(&ListHead);
      // abstracted to:
      // thisEntry = list[0];
      length--;

      ;
  }

  return 0;
}
